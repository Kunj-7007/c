#include <stdio.h>
void main()
{
    // Assignment op
    int a = 10;
    int b = 20;
    // a += 1;
    // a -= 1;
    // a *= 5;
    // Inc op
    // a++;
    // a--;
    a += (b * 2);
    printf("%d", a);
}